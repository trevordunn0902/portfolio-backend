# Application name
spring.application.name=portfolio-backend

# PostgreSQL datasource configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/portfolio_db
spring.datasource.username=portfolio_user
spring.datasource.password=TrevorDunn

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Show SQL queries in the console 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Default Spring Security user (for contact form auth)
spring.security.user.name=contactuser
spring.security.user.password=contactpassword123

# Admin login values (for managing skills)
admin.user=trevor
admin.password=mysecurepassword123
